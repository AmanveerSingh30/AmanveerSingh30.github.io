import { Routes } from '@angular/router';
import { HeartStageComponent } from './components/stages/heart-stage/heart-stage.component';

export const routes: Routes = [
  { path: 'heart-stage', component: HeartStageComponent }
];
